# G1 — أجوبة بوابة الفهم + سجلّ صيانة أودو (حزمة تسليم لجلسة الإصلاح)

> **الغرض:** مِلفّ واحد يُنقل لجلسة الإصلاح الرئيسية. يجمع (أ) تحليل موديول `ag_ishbek_inbound`
> بأدلّة مقتبَسة من الكود، و(ب) تلخيص التدخّلات المسجَّلة في ريبو `odoo-maintenance`.
>
> **تاريخ الإعداد:** 2026-07-14
>
> ### ⚠️ حدود الوصول في هذه الجلسة (اقرأها أولاً — تؤثّر على درجة الثقة)
> الجلسة مقيَّدة الصلاحيات على `engineeringjo-dev/almond` فقط. الموديول والريبو المطلوبان يقعان
> تحت مالك آخر (`almondcoffeehouse-stack`)، و`add_repo` رفض الإضافة (cross-tier غير مدعوم في v1).
> عملياً:
> - **`ag_ishbek_inbound`** يقع في `almondcoffeehouse-stack/Almond-Coffee-House` وهو **مفهرَس في بحث كود GitHub**،
>   لذلك أعدتُ بناء منطقه بالكامل عبر `search_code` (اقتباسات حرفية أدناه). **ثقة عالية** في أجوبة Q1–Q5.
> - **`almondcoffeehouse-stack/odoo-maintenance`** **غير مفهرَس** في بحث الكود إطلاقاً، وكل أدوات قراءة
>   المحتوى المباشرة (`get_file_contents` / `get_commit` / `list_commits` / `git clone`) محجوبة على هذه الجلسة.
>   المتاح الوحيد هو **`search_commits`** (رسائل كوميتات الفرع الافتراضي، من 2026-07-04 فصاعداً).
>   لذلك **جسم ملف سجلّ الصيانة نفسه (نص أواخر حزيران) لم أستطع قراءته حرفياً**؛ لخّصتُ ما أمكن من
>   رسائل الكوميتات وأشرتُ صراحةً لكل بند لم أتمكّن من التحقق منه.

---

## القسم أ — بوابة G1: الأسئلة الخمسة (أدلّة من `ag_ishbek_inbound`)

**الملفّات:** `ag_ishbek_inbound/__manifest__.py` · `ag_ishbek_inbound/models/ishbek_inbound.py` · `ag_ishbek_inbound/data/ishbek_cron.xml`
**ref المفهرَس:** `595aa5d…` — `version 19.0.1.0.0`، `depends = ["point_of_sale"]` فقط.

الترويسة التوثيقية أعلى الموديول تلخّص نيّته:
```python
# =========================================================
# ISHBEK INBOUND (كرون) — يسحب طلبات إشبيك وينشئ pos.order مسودّة.
#   - نفس منطق السكربت المحلي، لكن server-side عبر ir.cron.
#   - الأسرار من ir.config_parameter (يُدخلها المستخدم بإعدادات أودو):
#         ishbek.client_id · ishbek.client_secret
#     (لو غير مضبوطة → الكرون يتخطّى بهدوء، بلا خطأ).
#   - idempotent: pos_reference = {CHANNEL}-{orderId} (ما يكرّر).
#   - يُنشئ فقط إذا الكاش عنده جلسة مفتوحة.
# =========================================================
```

---

### Q1 — كيف يحلّ `cron_pull_orders()` طريقة الدفع لطلبات كريم/طلبات؟ وهل المطابقة حسّاسة للحالة/الشركة؟

**الجواب المختصر:** **بحثٌ بالاسم** (name lookup) — **ليس id ثابتاً، وليس من `config`/`ir.config_parameter`** — عبر
خريطة قناة→اسم مُصلَّبة في الكود، ثم فلترة بالاسم **بلا حساسية للحالة** داخل طرق دفع *نفس الكاش*.

**التسلسل:**

1) القناة تُحدَّد من `channelCode` في الطلب → `"CAREEM"` / `"TALABAT"` / وإلا `"MYTHINGS"`:
```python
CHANNEL_TO_PARTNER = {"CAREEM": 1556, "TALABAT": 1555, "MYTHINGS": 1557}
DEFAULT_PARTNER_ID = 1557     # MYTHINGS (تطبيق إشبيك المباشر) — افتراضي أي قناة غير معروفة
# طريقة دفع POS لكل قناة (بالاسم؛ تُلتقط من طرق دفع الكاش نفسه — تُطابَق بلا حساسية للحالة)
CHANNEL_TO_PAYMENT = {"CAREEM": "careem", "TALABAT": "talabat", "MYTHINGS": "MYTHINGS"}
```

2) الخريطة تُعطي **اسم** طريقة الدفع المستهدَف، ثم تُطابَق داخل `cfg.payment_method_ids` (طرق دفع ذلك الكاش تحديداً):
```python
# Careem→careem · Talabat→talabat · My Things→MYTHINGS (تُلتقط من طرق دفع الكاش نفسه).
pay_name = CHANNEL_TO_PAYMENT.get(channel, "MYTHINGS")
method = cfg.payment_method_ids.filtered(
    lambda m: (m.name or "").strip().lower() == pay_name.lower())[:1]
```

**الحسّاسية للحالة (case):** **غير حسّاسة.** الطرفان يمرّان بـ`.lower()` مع `.strip()` على اسم الطريقة.
هذا يجيب مباشرةً على ملاحظتك (توجد طرق باسم `careem` في شركتين و`Careem` في شركتين): **كلاهما يطابق**
`"careem".lower()` بلا فرق، فلا خطر من اختلاف الحرف الكبير/الصغير.

**الحسّاسية للشركة:** **آمنة بنيوياً بلا فحص شركة صريح.** الفلترة على `cfg.payment_method_ids` — أي طرق الدفع
المربوطة بذلك الـ`pos.config` (كاش الفرع)، وهو ينتمي لشركة واحدة. فلا وجود لبحثٍ عام قد يعبر الشركات؛ تُختار دائماً
طريقة الفرع/الشركة الصحيحة. و`[:1]` يأخذ أول تطابق (آمن ما لم يحمل نفس الكونفج طريقتين بالاسم ذاته).

**نقاط للفحص في جلسة الإصلاح:**
- لقناة MYTHINGS الاسم المستهدَف `"MYTHINGS"` → يبحث عن طريقة اسمها `mythings` (بلا حساسية للحالة). لو طريقة
  «My Things» على الكاش مُسمّاة بخلاف ذلك، لن يُطابَق → يُنشأ الطلب **بلا دفعة** (مسار `elif not method`).
- `[:1]` صامت عند التعدّد؛ لو كونفج فيه طريقتان بنفس الاسم، الاختيار غير محدَّد.

---

### Q2 — هل الموديول يقرأ أي حساب GL (receivable/outstanding/خاصية الجهة) أم يكتفي بتعيين الطريقة؟

**الجواب:** **يكتفي بتعيين طريقة الدفع فقط. لا يقرأ ولا يعيّن أي حساب GL إطلاقاً.**

**الأدلّة (سالبة، مؤكَّدة على مستوى الريبو):**
- `receivable` داخل الموديول → **0 نتيجة**. `account` داخل الموديول → **0 نتيجة**.
- `__manifest__.py`: `"depends": ["point_of_sale"]` — لا `account`، لا `l10n_jo_*`.
- قاموس إنشاء الطلب لا يحوي أي حقل حساب:
```python
order = self.env["pos.order"].create({
    "session_id": sess_id,
    "config_id": cfg.id,
    "company_id": company_id,
    "partner_id": cust_id,
    "pricelist_id": plist,
    "pos_reference": ref,
    "uuid": str(uuid.uuid4()),
    "date_order": datetime.now(timezone.utc).strftime("%Y-%m-%d %H:%M:%S"),
    "amount_tax": 0.0,
    "amount_total": round(total, 3),
    "amount_paid": 0.0,
    "amount_return": 0.0,
    "lines": lines_cmd,
})
```
- الدفعة تُنشأ بحقول الحد الأدنى فقط:
```python
order.add_payment({
    "amount": round(total, 3),
    "payment_method_id": method.id,
    "pos_order_id": order.id,
})
order.action_pos_order_paid()
```

**الخلاصة:** حَلّ الحسابات المحاسبية (receivable/outstanding للطريقة، ويوميات الجلسة) متروك بالكامل لأودو القياسي
عبر إعدادات طريقة الدفع والجلسة. الموديول يلمس **الطريقة** فقط ثم يُنهي بـ`action_pos_order_paid()`.

---

### Q3 — من يستهلك `default_pos_receivable_account_name` (نص على 26 طريقة)؟ هل يُقارَن نصّياً في أي مكان؟

**الجواب:** **لا أحد في هذا الريبو يستهلكه، ولا توجد أي مقارنة نصّية له.**

**الأدلّة (بحث على مستوى الريبو كامل، لا الموديول فقط):**
- `default_pos_receivable_account_name` → **0 نتيجة**
- `pos_receivable_account` → **0 نتيجة**
- `receivable_account_name` → **0 نتيجة**

الحقل النصّي (المضبوط على 26 طريقة دفع) هو عملياً **بيانات يتيمة** بالنسبة لهذا الكود المخصّص: لا يُقرأ، ولا يُقارَن
حرفياً، ولا يُستعمل لحلّ أي حساب. `ag_ishbek_inbound` تحديداً لا يمسّه.

**تحذير نطاق:** بحث الكود يغطّي الفرع المفهرَس من `Almond-Coffee-House` فقط. لو وُجد مستهلِك في وحدات أودو
الرسمية/المجتمعية أو في ريبو غير مفهرَس، فلن يظهر هنا. لكن **داخل الكود المخصّص لـAlmond: صفر مستهلِك.**
يُنصح في جلسة الإصلاح بالتأكد إن كان الحقل يُقرأ من طبقة SQL/تقرير خارجي قبل الاعتماد عليه.

---

### Q4 — هل يضبط علم JoFotara `l10n_jo_edi_pos_is_cash` عند إنشاء الطلب؟

**الجواب:** **لا. الموديول لا يضبط هذا العلم إطلاقاً.**

**الأدلّة (سالبة، على مستوى الريبو):**
- `l10n_jo_edi_pos_is_cash` → **0 نتيجة** في كل الريبو.
- `is_cash` داخل الموديول → **0 نتيجة**. (`is_cash` على مستوى الريبو يظهر فقط كحقل أودو القياسي
  `is_cash_count` في وحدات أخرى مثل `pos_external_api` و`agile_consulting_l10n_jo_edi_pos`.)
- قاموس الإنشاء (أعلاه) لا يضبطه، والموديول لا يعتمد إلا على `point_of_sale`.

**الأثر المهم لجلسة الإصلاح:** بما أن العلم لا يُضبط هنا، فتصنيف الطلب المُنشأ (نقدي مقابل آجل) لأغراض JoFotara
سيُحسَم من منطق وحدة JoFotara نفسها اعتماداً على **`payment_method_id.is_cash_count`** للطريقة المحلولة
(انظر `agile_consulting_l10n_jo_edi_pos/models/pos_order.py`):
```python
def _get_invoice_payment_method_code(self):
    """Get payment method code (1=cash, 2=card)"""
    has_cash = any(p.payment_method_id.is_cash_count for p in self.payment_ids)
    return '1' if has_cash else '2'
```
> نتيجةً لذلك: تصنيف كريم/طلبات كنقدي أو آجل في فاتورة JoFotara يتبع خاصية `is_cash_count` لطريقة الدفع
> التي حُلّت بالاسم في Q1 — لا علماً صريحاً يضبطه هذا الموديول. يستحق التأكيد الحيّ.

---

### Q5 — كيف يُضمَن عدم تكرار/فقدان الطلبات (idempotency) لو توقّف الكرون أو أُغلقت جلسة أثناء السحب؟

**الجواب:** عبر **مرجع فريد حتمي + فحص وجود قبل الإدراج + معاملة لكل طلب + حارس جلسة مفتوحة**.

1) **مرجع حتمي** مبني من القناة ومعرّف الطلب:
```python
ref = f"{channel}-{oid}"
```
(الترويسة: `idempotent: pos_reference = {CHANNEL}-{orderId} (ما يكرّر)`)

2) **حارس التكرار قبل الإنشاء** — ويُكتب المرجع على الطلب (`"pos_reference": ref`):
```python
if self.env["pos.order"].search_count([("pos_reference", "=", ref)]):
    return False
```
→ أي طلب سُحب سابقاً يُتخطّى.

3) **معاملة/rollback لكل طلب على حدة** — فشل طلب لا يلوّث البقية:
```python
try:
    if self._create_order(o, cfg, auth, pcache):
        created += 1
except Exception as e:
    self.env.cr.rollback()
    _logger.warning("Ishbek inbound: order create failed: %s", e)
```

4) **حارس الجلسة المفتوحة** — لا إنشاء إطلاقاً إن كان الكاش مغلقاً:
```python
if cfg.current_session_state != "opened" or not cfg.current_session_id:
    return 0
```
→ عند إغلاق الجلسة يُتخطّى السحب لذلك الكونفج كلّياً (لا يُنشَأ شيء). وبما أنه لا يُنشَأ شيء، لا يُفقَد شيء:
تُعاد الطلبات نفسها في نبضة الكرون التالية بعد فتح جلسة، وحارس التكرار يمنع الازدواج.

5) **نبضة مزدوجة للكرون** (تردّد فعلي ≈ 30 ثانية)، وحارس التكرار يجعل النبضة الثانية/أي تداخل آمناً:
```python
@api.model
def cron_pull_orders(self):
    """... نعمل نبضتين بفارق ~30 ثانية ليصير التردّد الفعلي ≈ 30 ثانية (شبه مباشر)."""
    self._pull_all()
    try:
        time.sleep(30)
    except Exception:  # noqa: BLE001
        return
    self._pull_all()
```

**سيناريوهات + مخاطر يجب أن تتحقّق منها جلسة الإصلاح:**
- **توقّف الكرون كلياً:** لا سحب حتى الاستئناف؛ لا فقدان **طالما** مصدر إشبيك يبقي الطلبات قابلةً للاستعلام بالتاريخ.
- **إغلاق جلسة أثناء السحب:** الطلبات المُنشأة (المرحَّلة) تبقى؛ الباقي يُتخطّى (`return 0`) ويُعاد لاحقاً — بلا ازدواج
  وبلا فقدان دائم.
- **⚠️ فجوة نافذة التاريخ:** الحلقة `for d in dates` تسحب ضمن نافذة تواريخ. لو كانت النافذة «اليوم فقط» وبقي فرع
  مغلقاً عبر منتصف الليل، فقد تسقط طلبات قديمة خارج النافذة قبل أن تُلتقط. **يجب التحقق من مدى النافذة (`dates`).**
- **⚠️ حبكة `time.sleep(30)`:** تمسك الـworker/المعاملة 30 ثانية. الحارس (`search_count`) يمنع الازدواج داخل النبضة
  الواحدة، لكن **لم أرصد `cr.commit()` صريحاً لكل طلب**؛ يعتمد ترحيل الطلبات على commit أودو نهاية الكرون. يستحق
  التأكد من دقّة تقطيع الالتزام (commit granularity) تحت انقطاع منتصف النبضة.

---

## القسم ب — تلخيص سجلّ صيانة أودو (`odoo-maintenance`)

> **مصدر هذا القسم:** رسائل الكوميتات على الفرع الافتراضي (2026-07-04 → 2026-07-13) عبر `search_commits`.
> **جسم ملف السجلّ (`ODOO_MAINTENANCE.md` وما قبله من عمل حزيران) لم يكن قابلاً للقراءة المباشرة في هذه الجلسة**
> (الريبو غير مفهرَس + أدوات المحتوى محجوبة). البنود المؤرّخة قبل 2026-07-04 مستنتَجة من إشارات كوميتات تموز، ومُعلَّمة.

### النيّة الحاكمة (من كوميت الخطة `5e8aafe`)
> «أوقف النزيف الجذري + احرس البوابات الزمنية → صحّح القاعدة الرقمية → افوتر فوق قاعدة سليمة → أغلق الضريبة».
الصيانة منظَّمة كـ«مستشفى»: تشخيص D1–D21 (Health Score خط أساس **30.1/100**)، ثم موجات Wave 0/1 بقرارات لجان خبراء
(CFO + محاسب تكاليف + مخزون + مهندس أودو + مدقّق EY) وتوكن + لقطات before/after + سطر تدقيق لكل كتابة.

### 1) حوكمة وبنية تحتية
- **2026-07-04** — إنشاء `ODOO_MAINTENANCE.md` (سجلّ الصيانة الحاكم: جدول حالة حيّ بـ7 معلّقات + سجلّ تحديثات). دفعة أولى:
  6 أدلّة فنية + مذكرة CFO + دليل تسلّم + سكربتات (الإصلاحات السبعة، دفعة الليل، فحص الوحدات، التقارير). المفتاح نُظّف لمتغيّر بيئة.
- **2026-07-11** — بروتوكول تزامن متعدد الأجهزة: pull تلقائي عند بدء الجلسة (hook) + إلزام `MAINTENANCE_LOG.md` عند كل تعديل + push فوري.
- **2026-07-11** — إصلاح سكربتات «الحارس» الثلاثة المفقودة (`block_master1.py` · `require_approval.py` · `checkpoint_ledger.py`)؛
  غيابها من git جعل خطّاف Bash يفشل مغلقاً ويمنع كل الأوامر (الحارس = الكاتب الوحيد).

### 2) POS / الجلسات / عدّ الكاش
- **2026-07-05** — فحص حيّ بوكيلين: دفعة الليل #4 متجمّدة (4,861؛ سبب تشغيلي، جلستان عالقتان) + كلفة BOM #5 (20 خام صفر الكلفة، 43 سطر ml/KG).
- **2026-07-05** — إغلاق shafa badran 2 (جلسة 3375 نظيفة بلا قيد) + تشخيص Al-Rabieh 2 (جلسة 3413: 93 طلب paid، فرق كاش −98.61 غير معدود — **لا إغلاق أعمى**).
- **2026-07-05** — فحص أداء بـ4 وكلاء: الجذر انتفاخ جداول (~90K إهلاك مستقبلي + 454K chatter + 5,775 كوانت سالب + 46K حركة عالقة) + هدر كرون #69 + لا أرشفة POS.
- **2026-07-05** — **تعطيل كرون #69 «POS COGS Posting» (هدر)** بعد تأكيد ترحيل المطبخ خارجي وأن COGS في #45.
- **W0-3 (2026-07-10)** — إلزام عدّ الكاش بحدّ **5 دنانير** على 14/14 كونفج (`set_maximum_difference=True` + `amount_authorized_diff=5.0`).
  اكتشاف: `cash_control` مفعّل شكلياً لكن بلا عتبة (الحد=0، الإلزام معطّل)؛ الإقفال الأعمى 73–100% بكل الفروع.
- **W0-7 (2026-07-10)** — إغلاق جلستين عالقتين فارغتين (`event/02722` + `Madinah street 2/02799`) + حلّ لغز كونفج 70/72.

### 3) التصنيع والمخزون (Wave 0/1)
- **W0-1 (2026-07-10)** — IC auto-validate → **Draft** بالشركات الأربع. D1: 2,029 كوانت سالب حقيقي؛ G1: تصنيع مفقود 57%.
- **W0-2 (2026-07-10)** — **تعطيل مولّد أوامر التصنيع** (`stock.rule` 114/117/120/123 `active=False`): MOs تُولَّد آلياً من مبيعات يومية عبر 4 مسارات MTO ولا أحد يقفلها (9,063 مفتوح).
- **2026-07-10** — **إلغاء 8,969 أمر تصنيع** (المفتوح 9,063 → 94). D13 (psql): صفر uuid مكرّر. D17: GRNI فعلي 624,930 دينار / 384 أمر.
- **W1-B2 (2026-07-11)** — إلغاء 22 استلام يناير يتيم (مسوّدات فارغة، صفر أثر).
- **W1-B6 (2026-07-11)** — إلغاء مرتجع التموين `CK/IN/00004` (لجنة ثلاثية: صنف تالف عمره 6 أشهر يلوّث AVCO الحيّ).
- **W1-B14 (2026-07-11)** — 6 تحويلات بشقيق done: مطابقة ثلاثية → إلغاء `Jamah/IN/00212` + ترحيل `Rabie/IN/00659` و`City/IN/00569`؛ تعليق حالات UoM متطرفة.
- **مراجعة عكسية (2026-07-11)** — لجنة (EY+بيانات+تكاليف) راجعت 16 كتابة؛ تأكيد إلغاءات `[RM]` (B3/B8/B13): الرابية ترسل عجين+صوص بيتزا `[SP]` جاهزين،
  وPOS الرابية يفجّرها لمكوّناتها = **انفجار مكرّر يستنزف مخزون الرابية (مصدر السوالب)**.
- **G0 (2026-07-13)** — حفر كود «فليب storable»: إصلاح صيانتنا (`85711aa`) لا تكامل مارق؛ 0 أتمتة/كرون تلمس `is_storable`؛ `write_date` مجمّد 8 أيام؛ إعادة التفعيل آمنة على staging بعد G1+G2.

### 4) البينيّة (Intercompany / المرايا / التسعير)
- **2026-07-05** — تدقيق مبيعات مكة + **إصلاح حساب توصيل كريم (تصنيف/reclass + 15 قيد تحليلي)** + تدقيق تغطية التحليلي بالشركات الأربع.
- **2026-07-05** — IC-PnL: وسم الفوترة البينيّة بالتحليلي للفرع (يونيو حيّ) + مواصفة + سكربت شهري.
- **2026-07-10/11/12** — قانون تسعير المالك بأربع حالات: داخل الشركة = قيد تحليلي فقط؛ بين الشركات = **كلفة +10% ربح +8% ضريبة** بتحليلي الفرع المشتري، مع منع الازدواج.
- **diag (2026-07-12)** — تشخيص تحويلات **TRF** البينية (يناير–مايو) عبر موقع «Customers» + نوع «PoS Orders» بلا `pos_order/sale` = **صفر قيد محاسبي (0/5302)**، `price_unit=0`، صفر تحليلي → الكلفة تتبخّر من الطرفين، ربح مصدر مبالَغ + مخزون منتفخ (~271K). الآلية المكسورة **توقّفت 2026-05-02** واستُبدلت بـ`sale.order` + فوترة IC شهرية.
- **0f8067e (2026-07-11)** — قرار المرايا البينيّة (لجنة ثلاثية): مرايا مستندية يدوية per-invoice (`account.move.create` draft، `auto_post=False`، 8% صريحة، بلا خصم/pricelist، `ref=INV`). تصنيف: 15 أُنشئ (133,934) / 2 تحقيق / **21 معلّق CFO** (153,690 عكس غير موثّق).
- **d3f6ec2 (2026-07-12)** — قاعدة المالك: إلغاء الفاتورة البينية **ثنائي الاتجاه** + تصنيف الـ26 الملغاة (18 Evora مرحَّلة بلا مرآة = بيع حقيقي يُصلَح بالإضافة · 3 متوازنة مكرّرة · 2 متزامنة · 3 بلا مرجع).
- **82dc09a (2026-07-12)** — **حارس المرايا البينية (IC-Watchdog)** يرصد قراءة-فقط: مرايا شبح صفرية، شطب أحادي، مرايا مكرّرة حيّة (تحقّق حيّ: شبح=2 معروفتان، أحادي=0، مكرّر=0).
- **0296f8d (2026-07-12)** — MEPS: تشخيص حجب IP + بناء relay؛ كتابة `pos.config(1).restrict_price_control=True` (منع تعديل السعر بمكة، طلب مالك).
- **0185653 (2026-07-12)** — تصحيح: نقل الرابية `[SP]` بعد 2/5 غير مستقر — الآلية الصحيحة اشتغلت للعجين **8 أيام فقط (17–24 حزيران)** ثم توقّفت؛ الصوص لم يُنقل؛ Evora لم يستلم. الجذر: `[SP]` `is_storable=False (consu)` → لا valuation → التحويلات لا تسجّل كلفة.

### 5) COGS اليتيم / zero-cost / تقرير الفهم الموحّد
- **W1-B (2026-07-11)** — فحص COGS يتيم لسلسلة الوزارة `S00007–S00013` = **506.70** (كلفة OUT 833.09 − إيراد مرحَّل 326.39). السبب: إلغاء 8 فواتير (6 مسوّدات + 2 مرحّلتان أُلغيتا 13/01 بلا استبدال). الرقم منقوص بسبب **علّة zero-cost** لأصناف FP.
- **diag** — لغز استلامات assigned بلا فاتورة: كرون #37 + orderpoints + Daily Procurement يؤكّد PO آلياً → OdooBot يحجز استلاماً 100% بلا فوترة → تراكم صامت. القرار: تصنيف بالسعر (0 → لا يُرحَّل).
- **f7a2320 (2026-07-13)** — تقرير الفهم الموحّد 100% (لجنة 6 خبراء). تصحيحات: رابية=4 مستودعات per-company · بَغ التوجيه = فشل تصديق (67 assigned) لا وجهة · **COGS بقيد POSS عبر `std_price` مجمّد 07-05** · السوالب تتذبذب لا تنمو (مجموع الكمية −28%).

---

### أواخر حزيران 2026 — الثلاثي المطلوب صراحةً (قيود IC reclass · تجربة COGS · إطفاء anglo_saxon)

> هذه التدخّلات وقعت **قبل أول كوميت في الريبو (2026-07-04)**، فهي مسجَّلة **نصّاً في جسم ملف السجلّ / التاريخ الطبي
> (`PATIENT_HISTORY_BRIEF.md` / `ODOO_MAINTENANCE.md`)** الذي **تعذّرت قراءته مباشرةً في هذه الجلسة**. ما يلي **استنتاج
> من إشارات كوميتات تموز فقط**، مع تعليم درجة الثقة — يجب على جلسة الإصلاح فتح جسم الملف للتأكيد الحرفي:

| البند | ما أمكن التحقق منه (من كوميتات تموز) | النيّة المرجَّحة | الثقة |
|---|---|---|---|
| **قيود IC reclass** | الآلية الصحيحة (sale.order + فاتورة + مرآة، فوترة IC شهرية) حلّت محلّ آلية TRF المكسورة التي توقّفت 2026-05-02؛ نقل الرابية `[SP]` اشتغل **17–24 حزيران**؛ «W0-0 منشور منذ 27/06»؛ وسم التحليلي للفوترة البينيّة «يونيو حيّ»؛ لاحقاً reclass حساب توصيل كريم (+15 قيد تحليلي). | إعادة تصنيف/توجيه القيود البينيّة لإصلاح P&L الفروع المشوّه ووقف تبخّر الكلفة. | متوسطة (السياق مؤكَّد، تفاصيل حزيران غير مقروءة حرفياً) |
| **تجربة COGS** | ترحيل COGS عبر **قيد POSS باستخدام `standard_price` مجمّد (07-05)**؛ كرون #69 «POS COGS Posting» جُرّب ثم **عُطّل 07-05** كهدر بعد تأكيد أن COGS يُعالَج في #45 والمطبخ خارجي. | تجربة ترحيل تكلفة البضاعة المباعة عند POS ثم التراجع لصالح مسار #45. | متوسطة–عالية (مؤكَّد أن الكرون عُطِّل وأن هناك مسار std_price مجمّد) |
| **إطفاء `anglo_saxon`** | **لا إشارة في أي رسالة كوميت** (`anglo` → 0 نتيجة). موجود فقط في جسم السجلّ غير المقروء. | (على الأرجح) إطفاء المحاسبة الأنجلوسكسونية/COGS الفوري لتفادي قيود تكلفة مشوّهة أثناء تنظيف القاعدة الرقمية — **غير مؤكَّد**. | **منخفضة — لم أتمكّن من التحقق. يجب فتح جسم `ODOO_MAINTENANCE.md`.** |

---

## ما تحتاجه جلسة الإصلاح لسدّ الفجوة
لقراءة جسم سجلّ الصيانة حرفياً (بنود أواخر حزيران، وتحديداً إطفاء `anglo_saxon`)، افتح جلسة مصدرها الأساسي
`almondcoffeehouse-stack/odoo-maintenance` (وأضف `Almond-Coffee-House` من نفس المالك)، لأن جلسة `engineeringjo-dev/almond`
الحالية لا تملك صلاحية قراءة محتوى ذلك الريبو ولا هو مفهرَس في بحث الكود. الملفات المستهدَفة:
`ODOO_MAINTENANCE.md` · `PATIENT_HISTORY_BRIEF.md` · `PENDING_ACTION_PLAN` · `IC_ANALYTIC_DESIGN`.
