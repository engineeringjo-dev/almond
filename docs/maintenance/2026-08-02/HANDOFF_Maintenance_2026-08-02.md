# 📋 ملف تسليم الصيانة — جلسة 2026-08-01 → 2026-08-02
**المالك:** حمزة · **البيئة:** إنتاج Odoo 19 (`ag-almond-coffee-house.odoo.com`) + ريبو الصيانة `engineeringjo-dev/production-almond`
**الغرض:** توثيق كل ما أُنجز + تعليمات تنفيذ المتبقي حرفياً (لجلسة معاذ أو أي جلسة كلود جديدة)

---

## القسم الأول — ما أُنجز (مثبت وموثق)

### 1. إصلاح POS فرع الجامعة (Access Error)
- الجذر: منتج «Mini Salmon Bagel» (id 10412) محصور على Evora + متاح بالـPOS ⇒ كسر تحميل الكتالوج لغير Evora.
- الحل: `company_id=False` — نُفّذ وتحقق: **0 منتجات POS محصورة**. لن تتكرر.

### 2. جرد الفروع الخمسة (مكة/المدينة/الرابية/خلدا/سيتي مول)
- تصفير كل صنف غير مجرود (موجب وسالب): 3,419 سطر، أثر مالي صافٍ 18,563.60 (شطب 27,580 / ربح 9,016).
- ثم **استرجاع كامل لأصناف المطبخ [FP]** بقرار المالك: 2,451 سطر رجعت لأرصدتها الأصلية بالفلس (تحقق: 0 متبقي).
- الصافي النهائي المطبق: 968 سطر غير FP، أثر 9,147 د.أ.
- باكابات: `jard_backup_full.json` + ملفا Excel سُلّما للمالك.

### 3. تضخم قيمة المستودعات (اكتشاف — لم يُنفذ تصحيح)
- 93% من قيمة مخزون الشبكة (760,376 من 815,747) **وهمي**: 26 صنفاً أُدخل جردها بالقطعة بينما وحدتها صندوق (Box 1000/BOX/Tea Box…).
- أبرزها: المدينة 336K، الجامعة 282K، مكة 75K (سليف كبير 1,500 «صندوق» والصحيح 1.5)، خلدا 59K.
- **قرار التصحيح عند إيهاب** — الملف التفصيلي: `UOM_Inventory_Inflation_2026-07-31.xlsx` (القسمة على معامل الصندوق لكل سطر).

### 4. عطل الفوترة البينية الشهرية — تشخيص لجنة 4 خبراء + إصلاح جذري
- **الجذور المؤكدة:** مهلة 20ث×3 محاولات=60ث بالضبط (الدالة لا تملك وقتها)؛ إجهاض HTTP لا يلغي معاملة أودو (إعادة الكتابة=ازدواج)؛ `invoice_date` لا يُضبط؛ Italian الأخير بالترتيب يُجوَّع دائماً؛ 3 طبقات صمت بالتنبيهات.
- **باغ الخصم المزدوج 46%:** حذف `pricelist_id` من create لا يحجب القائمة — أودو يسحب قائمة الشريك الافتراضية (9337 نفسها) فيُحسب 46% فوق السعر الصريح 0.54×list ⇒ فوترة تموز نقصت ~46%.
- **الإصلاح (مدموج في `main` عبر PR #67):** طابور خطوة/نداء (نمط export-daily)، تناوب مشترين، فواتير ≤800 سطر، ترحيل-قبل-إنشاء مع تبنّي المسودات، كتابة بلا إعادة، مهلات صريحة (60/240ث)، ختم التاريخ=آخر الشهر، تنبيه دائماً، maxDuration 300/قفل 310ث + `pricelist_id:false` و`discount:0` بالتصدير. اختبارات 1766/1766.
- الجدولة `* 1 1 * *` **لم تُمس** (شبكة إعادة المحاولة المقصودة — درس حوكمة سابق).

### 5. إنقاذ فوترة تموز البينية — 124,383.96 د.أ (منفذ بالكامل)
| المشترية | فواتير | الإجمالي |
|---|---|---|
| Leria | 9 (INV/2026/01319–01327) | 54,446.31 |
| Italian | 10 (INV/2026/01309–01318) | 43,579.01 |
| Almond | 1 (INV/2026/01308) | 26,358.65 |
- كلها بتاريخ **2026-07-31** وبالأسعار المصححة (تصفير الخصم المزدوج على أسطر الفواتير/الأوامر).
- INV/2026/01307 (الغلط: تاريخ 1/8 + أسعار ناقصة): عولجت **بإشعار دائن RINV/2026/00008** (مرحّل، payment_state=reversed) ثم أعيد الإصدار ضمن الفواتير أعلاه.
- أُلغيت مسودتان خطرتان عند Leria: المرآة المعطوبة 360543 (ازدواج ضريبي 16%) + **المكررة 355622 (51,782.49 = نسخة فاتورة حزيران المرحلة)**.

### 6. الفوترة الوطنية JoFotara — شُبكت ونُفّذ التصريف
- بيانات الاعتماد جاهزة أصلاً للشركات الأربع (وضع حقيقي). **فُعّل `l10n_jo_edi_pos_enabled=True` للشركات الأربع** (فواتير POS الجديدة تدخل الطابور تلقائياً).
- الكرونان المؤقتان (ir.cron 67/68 على pos.order) **تُركا معطلين عمداً** — التصريف المتحكم بدلهما.
- شرطان مسبقان أُصلحا: `preferred_payment_method_line_id` (يُملأ تلقائياً بالسكربت) + **`l10n_jo_edi_invoice_type='local'` مُلئ على 7,708 فاتورة** (جذر عالق «النوع الفارغ»).
- **النتيجة: 5,050 فاتورة Sent + QR رسمياً** (كانت 208). التفصيل: Evora 750 · Leria 2,729 · Almond 179 · Italian 1,391 (+1 بايلوت الخصومات).

### 7. حل الخصومات (الأسطر السالبة) — البايلوت ناجح
- البوابة ترفض أي سطر سالب ⇒ 2,886 فاتورة مخصومة عالقة.
- **الحل المعتمد من المالك (نفس حل التصدير اليومي):** حذف سطر Discount السالب وخبز قيمته بأسعار الأسطر الموجبة بعامل واحد — **الإجمالي بعد الخصم يبقى مطابقاً للفلس، نفس الرقم ونفس التاريخ**.
- **بايلوت منفذ على الإنتاج: INV/2026/01131** — إجمالي 3.596→3.596، Sent+QR. ✅
- المعالجة الجماعية + الكرون التلقائي: جاهزان، **لم يُنفذا** (القسم الثاني).

---

## القسم الثاني — المتبقي وتعليماته الحرفية

> بيانات الاتصال: `scratchpad/.odoo_env` (صيغة `export KEY=value`؛ ODOO_URL/ODOO_DB/ODOO_LOGIN/ODOO_API_KEY — حساب ALMOND API uid=41).
> مسار الـscratchpad: `/tmp/claude-0/-home-user-almond/ba59e94f-62c6-5206-8db2-95dabac525e0/scratchpad/`
> سجل التدقيق: `PROD_AUDIT.jsonl` — **أكمل التسجيل فيه لكل عملية كتابة.**

### أ) المعالجة الجماعية للخصومات التاريخية — 2,677 فاتورة not_paid ⭐ الأولوية
- السكربت جاهز ومجرب البنية: **`scratchpad/negfix_batch.py`** (متوازٍ 6 عمال، حارس إجمالي: أي عدم تطابق ⇒ استعادة كاملة من اللقطة وإعادة ترحيل كما كان، لا مسودات متروكة أبداً).
- التشغيل (جولات ~9 دقائق حتى النضوب):
  ```bash
  cd /tmp/claude-0/-home-user-almond/ba59e94f-62c6-5206-8db2-95dabac525e0/scratchpad
  python3 negfix_batch.py 510 6    # كرر حتى remaining≈0 (~10-12 جولة)
  ```
- الحالة/السجل/الباكابات: `negfix_state.json` · `negfix.log` · `negfix_backups.jsonl` (لقطة كاملة لكل فاتورة قبل اللمس).
- قائمة الفواتير تُشتق من `jofotara_drain_state.json` (مفاتيح errors التي نصها يحوي "negative").

### ب) الحل التلقائي الدائم (الخصم يومي) — كود جاهز غير ملتزم
- الفرع: `feat/jofotara-negfix` في `/workspace/production-almond` — **الملفات مكتوبة بمجلد العمل، تحتاج commit**:
  - `src/services/odoo/jofotara-negfix.ts` (الخدمة: دوال نقية + معالجة فاتورة كاملة بحراسها)
  - `src/app/api/odoo/jofotara-negfix/route.ts` (كرون: قفل 310ث، 40 فاتورة/جولة، أثر `jofotara_negfix`)
  - `src/services/odoo/jofotara-negfix.test.ts` (اختبارات الدوال النقية)
  - `vercel.json` (أُضيف `/api/odoo/jofotara-negfix` بجدولة `*/10 * * * *` — إضافة فقط، لا تعديل على القائم)
  - `src/services/monitor/cron-health.ts` (أُضيف للمراقَبة: key `jofotara_negfix`, maxAgeHours 1)
- خطوات الإتمام:
  ```bash
  cd /workspace/production-almond
  git add src/services/odoo/jofotara-negfix.ts src/services/odoo/jofotara-negfix.test.ts \
    src/app/api/odoo/jofotara-negfix/route.ts src/services/monitor/cron-health.ts vercel.json
  git commit   # رسالة جاهزة في تاريخ المحادثة؛ المهم ذكر: نفس حل التصدير، بايلوت INV/2026/01131
  git fetch origin main:refs/remotes/origin/main && git rebase origin/main   # main يتقدم بسرعة (جلسة معاذ)
  npx vitest run && npx tsc --noEmit   # كله يجب أن يمر (أخطاء scripts/ و invoices/ قديمة غير ذات صلة)
  git push -u origin feat/jofotara-negfix   # ثم PR إلى main ودمجه
  ```
- ⚠️ ملاحظة كانت سبب فشل سابق: `*/` داخل تعليق JSDoc يقفل التعليق — لا تكتب الجدولة حرفياً بالتعليقات.

### ج) نشر Vercel معطل كلياً — Ignored Build Step 🔴
- **كل نشرات المشروع تُلغى** (حتى production من main): لوحة Vercel ← مشروع production-almond ← **Settings ← Git ← Ignored Build Step ← امسحه** ← Redeploy لآخر كوميت main.
- بدون هذا: إصلاح ic-invoicing المدموج + كرون negfix لن يعملا. (يُحتمل أن معاذ وضعه عمداً كتجميد — نسّق معه.)
- team: `almond-k` · project: `production-almond` (prj_YQIYsbfNc9ajnuhYcJRCISLhIKuH).

### د) بقايا حوض أخطاء JoFotara (بعد التصريف)
| الفئة | العدد | المعالجة |
|---|---|---|
| أسطر سالبة **مدفوعة/جزئية/معكوسة** | 209 (163 paid+6 partial+40 reversed) | نفس حل الخبز لكن **بعد** فك/إعادة التسويات يدوياً أو بمعالجة تحفظ المطابقات — لا تُعالج آلياً بالسكربت الحالي |
| UUID مكرر («provide the correct UUID») | 81 | غالباً مُبلغة سابقاً بقناة أخرى — طابق مع بوابة الدائرة، وإن كانت مسجلة علّمها داخلياً |
| سعر وحدة مفقود (أسطر بسعر 0) | 12 | صحح سعر السطر (أو احذف السطر الصفري إن كان زائداً) ثم أرسل |
| فردية | 13 | منها: RINV/2026/00008 (رفضته البوابة برسالة تاريخ الإشعار/الأصل)، فاتورة بضريبتين على سطر (INV/2026/01302)، INV/2026/01285 و01289 (NOT_SUBMITTED خام) — راجع كل واحدة من `jofotara_drain_state.json` |
- الملخص الجاهز: `jofotara_errors_summary.json`. يُطلب Excel لإيهاب من هذه البيانات.

### هـ) قرارات إيهاب المعلقة (موثقة بتقارير الجلسة)
1. تصحيح تضخم UoM (759 ألف وهمي) — الملف جاهز (§3).
2. الفواتير البينية المرآة عند المشترين (تتولد draft) — ترحيلها بعد المطابقة؛ ومطابقة فرق حزيران 29.589 (Italian).
3. إنشاء ضريبة شراء بينية غير شاملة لشركة Italian + إصلاح الوضعيات الضريبية FP13/FP14 (سبب اجتماع ضريبتين على سطر واحد بالمرايا) + وضعية بينية في Evora (66→722 عند الحاجة).
4. تقسيم الفواتير الشهرية (~8/مشترٍ) — أو أسبوعي.
5. الكتلة التاريخية أُرسلت فعلاً بقرار المالك — أبلغ إيهاب لمواءمة الإقرارات.

### و) ملفات معلقة عند معاذ
1. **أوامر APP- اليومية واقفة منذ 28/7** — تعديل تقطيع الكرون قتل التصدير (كوميتات جلسته على main توثق ذلك + أداة replay بحوزته `DATES=…` dry-run). أيام 28–31 تموز + آب تحتاج إعادة تشغيل، وإلا لا أساس لفوترة آب.
2. **فوترة البيادر (APP-WH)** — قاعدة المالك: كل إخراج من البيادر يصدر بفاتورة؛ فروع Evora قيد تحليلي؛ الشركات الأخرى **تكلفة+15% ثم ضريبة الصنف تضاف فوق المجموع** (نموذج غير شامل 722/723/724). مولد APP-WH خارج ريبو الصيانة (جلسة معاذ) — التطوير عنده أو يُبنى كرون قارئ للمستندات في production-almond.
3. مين رحّل 01307 يوم 1/8 الساعة 12:14 (جلسته على الأرجح) — للتوثيق فقط، عولجت.
4. فاتورة Italian متبقية واحدة to_invoice (~65 د.أ، مرجع غير APP/AGG) — فوترها يدوياً إن لزم.

### ز) ملاحظات تشغيلية للجلسة المنفذة
- **قاعدة السماح جاهزة** في `/home/user/almond/.claude/settings.local.json` — تُفعَّل تلقائياً في أي **جلسة جديدة** على هذا الريبو (المشكلة كانت فقط أن الجلسة الحالية بدأت قبل إنشائها).
- سكربتات جاهزة بالـscratchpad: `jofotara_drain_mt.py` (تصريف متوازٍ عام — أعد تشغيله بعد أي إصلاحات: يلتقط to_send الجديد تلقائياً) · `negfix_batch.py` · حالاتها `*_state.json`.
- بعد نضوب (أ): شغّل جولة `python3 jofotara_drain_mt.py 500 8` أخيرة لالتقاط أي متبق.
- تحقق ختامي مقترح: عدّ `to_send` لكل شركة ≈ 0 باستثناء فئات (د) الموثقة.

---
*أُعد بجلسة كلود بتفويضات حرفية من المالك حمزة — كل عمليات الكتابة مسجلة في `PROD_AUDIT.jsonl` مع نص الموافقة.*
