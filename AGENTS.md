# AGENTS — قواعدُ العمل على مستودعات ALMOND

## ⚠️ اقرأ هذا قبل أوّل أمر

قسمُ **«أخطاءٌ وقعت فعلاً — قواعدُ منعِ التكرار»** (ثلاثةَ عشرَ بنداً) هو المرجعُ الحاكم،
ومكانُه `CLAUDE.md` في مستودع **`production-almond`**. كلُّ بندٍ فيه **حادثةٌ حقيقيّة بكلفتها
المقيسة** لا نصيحةً عامّة — والأخطاءُ نفسُها تكرّرت عبر جلساتٍ متعدّدة على **كلا** المستودعين.

الخلاصةُ التنفيذيّة، وهي كلُّها تنطبق هنا أيضاً:

### قبل أن تكتب سطراً

```bash
# ١) الفرعُ الافتراضيّ قد لا يكون حيث يجري العمل
git fetch --force origin "+refs/heads/main:refs/remotes/origin/main"
git rev-list --count HEAD..origin/main      # ≠ 0 ⇒ أنت على شجرةٍ قديمة

# ٢) `git fetch` قد يخدم مرجعاً قديماً — `ls-remote` هو الحَكَم
git ls-remote origin refs/heads/main | cut -f1
git rev-parse origin/main                   # اختلفا ⇒ اجلب بالقوّة كما أعلاه
```

بندٌ واحدٌ من هذين كلّف هيكلةً كاملة: ثمانون ملفّاً، بوّابةٌ خضراء، مدفوعة — رُميت وأُعيدت
من الصفر لأنّها بُنيت على شجرةٍ عمرُها شهر.

### القواعدُ الثلاثَ عشرة، مختصرةً

| # | القاعدة |
|---|---|
| ١ | الفرعُ الافتراضيّ ليس حيث يجري العمل — تحقّق قبل التفرّع |
| ٢ | لا تفترض تكافؤ نسختين — قِسْه على آلاف المدخلات، وثبّت القياسَ اختباراً |
| ٣ | السكربتُ الميكانيكيّ يفشل بصوتٍ عالٍ لا بصمت — ولا يكتب إلّا بعد نجاح خطواته كلِّها |
| ٤ | `rglob("src/**")` في بايثون يعني `**/src/**` — اشتقّ الملفّات من `git ls-files` |
| ٥ | لا تقارن حقولَ الرأس؛ اجمع من السطور، وتحقّق على صفٍّ واحدٍ قبل التعميم |
| ٦ | لا تُعمِّم من عيّنة — أو اذكر حجمها في الجملة نفسها |
| ٧ | قبل أن تشترط شرطاً، قِس على بيانات الإنتاج أنّه **يقع** |
| ٨ | اختبارٌ لا يحمرّ لا يحرس شيئاً — اكسر الحارسَ عمداً ثمّ استعده |
| ٩ | الحقيقةُ المكتوبةُ مرّتين (SQL وTypeScript) تنحرف — اكتب اختبارَ تطابق |
| ١٠ | لا `pkill`/`pgrep` بنمطٍ في هذه البيئة — يقتل الصدفةَ الأمّ |
| ١١ | صحّح بالقياس لا بالاعتذار: الرقمُ الصحيحُ وسببُ الفرق في جملتين، وامضِ |
| ١٢ | `git fetch` قد يخدم مرجعاً قديماً — `ls-remote` الحَكَم، ولا إجبارَ على `main` |
| ١٣ | جلساتٌ متوازية تكتب أثناء عملك — اجلب قبل الدفع، واحسب التعارض قبل فتح PR |

---

## خاصٌّ بهذا المستودع

مستودعُ **الصيانة والتكاملات** (وحدات Odoo · أدوات JoFotara · وثائق التسليم) — لا يُخلَط
بتطبيق الإنتاج `production-almond`.

### 🔒 حُرّاسٌ إنتاجيّة لا تُمَسّ

- ⛔ **لا تُفعّل `l10n_jo_edi_pos_enabled`** — مسارٌ موازٍ يُنتج تسجيلاً مضاعفاً لدى دائرة الضريبة.
- ⛔ **لا تُلغِ تثبيت `l10n_jo_edi_pos`** — ٣٩٬٣٠٤ طلباً (٤١٩٬٥٣٤ د.أ) تحمل في حقوله **دليلَ
  تسجيلها الوحيد** (رمز QR)، ومعها ٣٩٬٤٣٣ مرفقَ XML.
- 🔑 الاعتمادات في `scratchpad/.odoo_env` فقط — **لا تُودَع في git أبداً**.
- 🏢 الشركاتُ الأربعُ **مكلَّفون مستقلّون**: التقاصُّ بينها ممنوع.
- ✍️ أيُّ كتابةٍ على الإنتاج تحتاج **جملةَ موافقةٍ صريحةً باسم المالك**؛ وما دونها قراءةٌ فقط
  (`search` · `read` · `search_read` · `search_count` · `fields_get`).

**والقيدُ الحاكمُ فوق كلّ ما سبق:** لا يتأثّر البيعُ الجاري ولا يتعطّل الـPOS في الفروع.
