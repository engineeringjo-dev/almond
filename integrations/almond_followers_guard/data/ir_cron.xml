<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <record id="ir_cron_gc_staff_followers" model="ir.cron">
            <field name="name">Followers Guard: purge staff followers on POS/Loyalty</field>
            <field name="model_id" ref="mail.model_mail_followers"/>
            <field name="state">code</field>
            <field name="code">model._gc_staff_followers()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <!-- Anchor the daily run at 00:00 UTC = 03:00 Asia/Amman, when all
                 branches are closed; without this the run time drifts to
                 whatever hour the module was installed. -->
            <field name="nextcall"
                   eval="(DateTime.now().replace(hour=0, minute=0, second=0, microsecond=0)
                          + timedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')"/>
            <field name="active" eval="True"/>
        </record>
    </data>
</odoo>
